<div class="d-flex justify-content-center">
    <h1>Ninja List</h1>
</div>

<div class="container">
    {{#each ninjas}}
        <ul class="list-group">
            <li class="list-group-item">
                {{ this.name }} - {{ this.rank }}
            </li>
        </ul>
    {{/each}}
</div>